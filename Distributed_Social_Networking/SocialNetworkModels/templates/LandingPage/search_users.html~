{% extends "index.html" %}

{%block head%}

{% include 'navigationbar.html' %}
{% endblock %}
{% block body %}
    {% for author in authors %}
                    <div class="link">
                        <h4>{{ author.user.username }}</h4>
                    </div>

                    {% if author.user.username not in followed %}
                    <div>
                        <a href="/follow/{{ author.user.pk}}">
                            Follow
                        </a>
                    </div>

                    {% else %}

                    <div>
                        <a href="#">
                            UnFollow
                        </a>
                    </div>

                    {% endif %}
    {% endfor %}

    <p> eo <br/>

        (% for afriend in allfriends %}


        we need to compare our friends with each follow
        {{afriend}}

        {% endfor %}

    </p>


{%endblock%}
