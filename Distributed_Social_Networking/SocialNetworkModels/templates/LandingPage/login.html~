{% extends "index.html" %}

{%block head%}
        {% if user.is_authenticated %}
            <a class="navbar-brand" href="#">hello {{ user.username }}!</a>
            <a class="navbar-brand" href="/post/">CreateNewPost</a>
            <a class="navbar-brand" href="/logout/">Logout</a>
        {% endif %}
{%endblock%}

{%block body%}

        </br>
        </br>
        <div class="center-block" id="user-login">
         {% if error %}
             <h1 class="form-signin-heading">Please sign in</h1>
             {%else%}
             <h1 class="form-signin-heading">Not match</h1>
        {% endif %}
           
                <form role="form" method="post">
                    {% csrf_token %}
                <input type="text" name="username" class="form-control" placeholder="Username" required autofocus />
                <input type="password" name="password" class="form-control" placeholder="Password" required />
                <button class="login-submit" type="submit">Sign in</button>
            </form>
        </div>
        <div class = "register">New user <a href="/register">Register</a></div>
     

{%endblock%}
